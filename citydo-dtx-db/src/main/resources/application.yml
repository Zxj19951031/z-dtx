server:
  port: 8081
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/dtx-db?useSSL=false
    username: root
    password: 123456
    driver-class-name: com.mysql.jdbc.Driver

logging:
  level:
    ch.qos.logback.core: warn

mybatis:
  configuration:
    map-underscore-to-camel-case: true